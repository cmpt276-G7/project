<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= @user.name %> <%= @user.email %>
        <p> main page after log in, customizable to user settings soon (tm) </p>
      </h1>
    </section>
  </aside>
</div>
